import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "AI-Based Collaborative Discussion Analyzer | B.Tech Major Project",
  description: "Transform unstructured team discussions into structured insights with AI-powered analysis. Extract summaries, decisions, and action items automatically.",
  keywords: ["AI", "Discussion Analyzer", "Team Collaboration", "NLP", "B.Tech Project", "Major Project"],
  authors: [{ name: "Alokit Mishra" }],
  icons: {
    icon: "/logo.svg",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
